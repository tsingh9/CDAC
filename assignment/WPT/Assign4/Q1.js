function processData(str, fun){
           
    var c=fun(str);
    }

    var str="hello";
   processData(str,function toUppercase(str){console.log(str.toUpperCase())});

   var a=[1,2,3,4,4];
    function forEachElement(str,call){

        call(str);
    }